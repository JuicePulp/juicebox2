[ui]
host = "0.0.0.0"
port = 6400

# UI working directory override. Defaults to auto-detect.
# Config-file only.
# dir = "crates/juicefront/ui"

[sentry]
# DSN is env-only: SENTRY_DSN (or SENTRY_DSN_UI / SENTRY_DSN_JUICEFRONT).
environment = "production"
